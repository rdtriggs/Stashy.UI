import { vuex as core } from './core'
// import { vuex as dashboard } from './dashboard'
import { vuex as portfolio } from './portfolio'
import { vuex as settings } from './settings'

export default {
  core,
  // dashboard,
  portfolio,
  settings
}
